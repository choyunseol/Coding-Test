#include <stdio.h>

int main()
{
  printf("Hello");
  return 0;
}

---

https://codeup.kr/problem.php?id=1001
- #include : 라이브러리를 불러주는 코드
- stdio.h : 표준 입출력에 관련된 코드들이 들어 있는 라이브러리
- h : header의 약자. 라이브러리들은 모두 .h의 확장자를 가지고 있는 헤더파일
- int main(void : main 함수. 프로그램이 실행될 때 가장 처음으로 (main으로) 실행되는 부분.
- return 0 : 프로그램의 종료를 알리는 코드. return은 함수에서 반환값.

주의사항
- 세미콜론은 항상 모든 코드가 마칠때 적어주기
- 모든 코드는 항상 {} 중괄호 안에 포함
